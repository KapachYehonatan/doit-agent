Part2CommandPlanner[@T]: {
    S: {
        COMMAND_PLANNER_INSTRUCTIONS
        COMMAND_PLANNER_OUTPUT_FORMAT
    }
    U: env.user_instruction[@T]
}

Part2SafetyJudge[@T]: {
    S: {
        SAFETY_CHECK_INSTRUCTIONS
        SAFETY_CHECK_OUTPUT_FORMAT
    }
    U: resp.generated_command[@T]
}
